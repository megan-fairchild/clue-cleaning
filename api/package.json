{
  "name": "fairchild-manor-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/functions/*.js",
  "scripts": {
    "start": "func start",
    "build": "esbuild src/functions/*.ts --bundle --platform=node --target=node20 --format=esm --outdir=dist/functions --external:@azure/functions --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "@azure/cosmos": "^4.0.0",
    "@types/jsonwebtoken": "^9.0.0",
    "esbuild": "^0.28.0",
    "jsonwebtoken": "^9.0.0",
    "typescript": "~5.6.0"
  }
}
